export const mainTitleData = {
  elementSelector: `.intro__title`,
  duration: 500,
  interval: 350,
  letterDelay: 150,
  activeClass: `is-animate`,
  transitionProperty: `transform`,
};

export const introDateData = {
  elementSelector: `.intro__date`,
  duration: 300,
  interval: 100,
  letterDelay: 125,
  activeClass: `is-animate`,
  transitionProperty: `transform`,
};

export const storyTitleData = {
  elementSelector: `.story-title`,
  duration: 350,
  interval: 100,
  letterDelay: 125,
  activeClass: `is-animate`,
  transitionProperty: `transform`,
};

export const prizesTitleData = {
  elementSelector: `.prizes__title`,
  duration: 300,
  interval: 100,
  letterDelay: 125,
  activeClass: `is-animate`,
  transitionProperty: `transform`,
};

export const rulesTitleData = {
  elementSelector: `.rules__title`,
  duration: 300,
  interval: 100,
  letterDelay: 125,
  activeClass: `is-animate`,
  transitionProperty: `transform`,
};

export const gameTitleData = {
  elementSelector: `.game__title`,
  duration: 300,
  interval: 100,
  letterDelay: 125,
  activeClass: `is-animate`,
  transitionProperty: `transform`,
};
